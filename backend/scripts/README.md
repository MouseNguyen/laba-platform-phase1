# Scripts

## Generate Secrets

Use this script to generate secure random secrets for your `.env` file.

```bash
npm run generate-secrets
```

Or append directly to `.env`:

```bash
npm run generate-secrets >> .env
```
